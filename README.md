# cifar10-classifier

Simple TensorFlow / Keras classifier for the CIFAR-10 dataset.
- Train script: src/train.py

Usage (PowerShell):
```
python src\train.py --epochs 20 --batch-size 128 --save models\cifar10_best.h5
```